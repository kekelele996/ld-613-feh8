name: stage-light
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME:-stage-light}-frontend
    env_file:
      - .env
    ports:
      - "${FRONTEND_PORT:-20113}:80"
