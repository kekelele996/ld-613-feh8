# ld-613 舞台灯光编排模拟器

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**舞台灯光编排模拟器（stage-light）**：纯前端舞台灯光编排工具，支持灯具通道、场景 Cue、时间轴预览和演出方案导出，所有数据存在 IndexedDB。项目类型：**Web 前端应用**。

### 2. 核心数据模型（≥ 3 个，必须贯穿 store/API/组件/页面）

1. **灯具（Fixture）**
   - 字段：id, fixture_code, fixture_type, position_x, position_y, dmx_address, channel_count, color_mode
   - 关键关系：被 CueScene 和 TimelineTrack 引用
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

2. **灯光场景（CueScene）**
   - 字段：id, name, fixture_states, fade_in_ms, hold_ms, priority, scene_status
   - 关键关系：组成演出时间轴
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

3. **时间轴轨道（TimelineTrack）**
   - 字段：id, cue_scene_id, start_ms, duration_ms, layer, locked
   - 关键关系：控制场景播放顺序
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

4. **演出方案（ShowProject）**
   - 字段：id, title, venue_name, fixture_ids, track_ids, updated_at
   - 关键关系：聚合灯具和轨道
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

### 3. 核心页面（≥ 4 个，必须有路由配置）

1. **灯具布置** `/fixtures`
   - 消费实体/模型：Fixture + ShowProject
   - 关键交互：灯具平面图、DMX 地址、通道设置
   - 共享组件：FixtureIcon, DmxBadge, PropertyPanel

2. **场景编辑** `/cues`
   - 消费实体/模型：CueScene + Fixture
   - 关键交互：选择灯具、设置颜色亮度、渐变时间
   - 共享组件：CueCard, FixtureIcon, ColorChannelSlider

3. **时间轴编排** `/timeline`
   - 消费实体/模型：TimelineTrack + CueScene + ShowProject
   - 关键交互：拖拽场景、预览播放、轨道锁定
   - 共享组件：TimelineRuler, CueCard, StatusBadge

4. **舞台预览** `/preview`
   - 消费实体/模型：Fixture + CueScene + TimelineTrack
   - 关键交互：二维舞台预览、按时间播放灯光变化
   - 共享组件：StageCanvas, TimelineRuler, PlaybackControls

### 4. 共享枚举/常量（≥ 2 组）

1. **FixtureType**：PAR / SPOT / WASH / BEAM / STROBE
   - 常量位置：`frontend/src/constants/FixtureType.ts` 或同名模块
   - 类型位置：`frontend/src/types/FixtureType.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
2. **CueStatus**：DRAFT / READY / DISABLED / ARCHIVED
   - 常量位置：`frontend/src/constants/CueStatus.ts` 或同名模块
   - 类型位置：`frontend/src/types/CueStatus.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
3. **ChannelMode**：RGB / RGBW / DIMMER_ONLY / MOVING_HEAD
   - 常量位置：`frontend/src/constants/ChannelMode.ts` 或同名模块
   - 类型位置：`frontend/src/types/ChannelMode.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置

### 5. 共享前端组件清单（≥ 3 个）

- `FixtureIcon`
- `CueCard`
- `TimelineRuler`
- `StageCanvas`
- `ColorChannelSlider`

### 6. 共享 hooks/utils 清单（≥ 2 个）

- `useTimelinePlayback`
- `useDmxAddressCheck`
- `useIndexedDbStore`

### 7. 技术要求

- 英文短名（用于 Docker Compose）：`stage-light`
- 前端端口：`20113`
- 前端技术栈：React 18 + TypeScript + Vite + Tailwind CSS + Redux Toolkit + IndexedDB
- 状态管理必须使用独立 store，禁止全部写在组件内部 state
- 数据来源：localStorage / IndexedDB / 本地 mock 文件，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

前端必须包含：
```text
frontend/src/
├── api/                  # 即便是本地模拟，也要按模型分文件封装 async API
├── stores/               # Pinia/Zustand/Redux 独立 store，禁止全放组件 state
├── types/                # 数据模型类型定义
├── constants/            # 枚举、日志模板、错误信息、状态文案
├── constructors/         # 默认对象、表单对象、导入数据构造器
├── components/common/    # 至少 3 个共享组件
├── hooks/                # 至少 2 个自定义 hook/composable
├── pages/                # 至少 4 个路由页面
├── router/
├── utils/
├── mocks/
└── workers/              # 如有耗时计算，放 Web Worker
```

严禁合并职责到单一文件。


### 部署要求

本项目虽然是纯前端，也必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，只编排 `frontend` 服务，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=stage-light`、`FRONTEND_PORT=20113`
- 根目录 `.env.example`
- `frontend/Dockerfile`：多阶段构建，最终用 Nginx 托管
- `frontend/nginx.conf`：配置 `try_files $uri $uri/ /index.html;` 支持 SPA 路由
- 本地模拟数据放在 `frontend/src/mocks/`，禁止接入第三方 API

`docker-compose.yml` 必须满足：
- 顶层 `name: stage-light`
- `container_name: ${COMPOSE_PROJECT_NAME:-stage-light}-frontend`
- 前端端口映射：`${FRONTEND_PORT:-20113}:80`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d` 后访问 `http://localhost:20113`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
